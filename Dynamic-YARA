detect
event: json
type: point
op: and
rules:
  - op: is
    path: event/ext_name
    value: ext-yara
  - op: matches
    path: event/data/rule_name
    # This regular expression ensures ANY rule starting exactly with "my-rule" triggers it
    re: ^my-rule
target: deployment

response 
- action: task
  command: history_isolate
  suppression:
    is_global: false
    keys:
      - '{{ .routing.sid }}'
      - yara-isolation-suppression
    max_count: 1
    period: 1h
